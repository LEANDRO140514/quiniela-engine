{
  "name": "@curdeeclau/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Algorithmus Platform canonical runtime contracts — provider-agnostic, multitenant-ready",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0",
    "vitest": "^2.1.0"
  }
}
