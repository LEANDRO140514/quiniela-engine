{
  "name": "@curdeeclau/crm-engine",
  "version": "0.1.0",
  "private": true,
  "description": "Provider-agnostic CRM Runtime Engine — contacts, opportunities, pipelines, campaigns, tags",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@curdeeclau/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0",
    "vitest": "^2.1.0"
  }
}
